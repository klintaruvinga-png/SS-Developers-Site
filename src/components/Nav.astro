---
const pathname = Astro.url.pathname.replace(/\/$/, '') || '/';
const navItems = [
  { href: '/', label: 'Home' },
  { href: '/projects', label: 'Projects' },
  { href: '/how-i-work', label: 'How I work' },
  { href: '/work-with-me', label: 'Work with me' },
  { href: '/contact', label: 'Contact' },
  { href: '/now', label: 'Now' },
];
---
<header class="site-nav">
  <div class="nav-inner">
    <a href="/" class="brand-lockup" aria-label="SS Developers home">
      <img
        src="/assets/ss-dev-mark.png"
        alt=""
        class="brand-emblem"
        width="224"
        height="224"
        aria-hidden="true"
      />
    </a>

    <nav class="nav-links nav-links-desktop" aria-label="Primary">
      {navItems.map((item) => {
        const normalizedHref = item.href.replace(/\/$/, '') || '/';
        return (
          <a href={item.href} aria-current={pathname === normalizedHref ? 'page' : undefined}>
            {item.label}
          </a>
        );
      })}
    </nav>

    <a href="/contact" class="nav-cta nav-cta-desktop">Start Your Project</a>

    <details class="nav-mobile">
      <summary>Menu</summary>
      <div class="nav-mobile-panel">
        <nav class="nav-mobile-links" aria-label="Mobile">
          {navItems.map((item) => {
            const normalizedHref = item.href.replace(/\/$/, '') || '/';
            return (
              <a href={item.href} aria-current={pathname === normalizedHref ? 'page' : undefined}>
                {item.label}
              </a>
            );
          })}
        </nav>
        <a href="/contact" class="button-primary">Start Your Project</a>
      </div>
    </details>
  </div>
</header>
